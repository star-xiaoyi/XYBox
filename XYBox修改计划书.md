# XYBox 修改计划书

> 文档版本：1.1
> 制定日期：2026-08-18  
> 当前代码基线：`6244238 chore: initialize buildable XYBox baseline`

## 1. 项目目标

在现有 XMBOX 3.1.6 代码基础上继续维护 XYBox，不进行无目的的大规模重写。第一阶段产品形态确定为：

- 只发布一个触屏 Android APK，同时适配手机和平板。
- 根据当前窗口宽度、横竖屏和可用空间自动调整内容密度与布局，不再让用户选择“手机版”或“平板版”。
- 当前只维护触屏端；电视端保留未来独立实现的可能，但不进入本轮开发和发布范围。
- 保留现有播放器、CatVod、QuickJS、直播、下载等已经能工作的核心能力，修改围绕交互、同步、主题、UI 和已知缺陷展开。
- 不整体迁移到 FongMi 的新架构。后续如确有需要，只按功能逐项参考或移植，避免为了“更新架构”破坏现有完整功能。

## 2. 第一性原则

本轮修改遵循以下原则：

1. **数据安全优先**：同步功能宁可明确失败，也不能静默覆盖较新的观看记录或设置。
2. **单一事实来源**：手机和平板共用一套业务代码和主布局，不维护两份几乎相同的实现。
3. **按窗口适配**：判断依据是 App 当前可用宽度，不是设备名称或“是不是平板”；分屏、折叠屏和横竖屏都应自然适配。
4. **设置必须真实生效**：界面上出现的开关必须接入实际执行路径，不能只保存一个配置值。
5. **交互动作与控制层分离**：双击暂停、快进提示等瞬时动作不应强制打开整套播放器控制层。
6. **每一阶段都可回退**：每个阶段单独提交、单独编译、单独验收，不把架构迁移、UI 改版和数据格式变更混在一次提交里。
7. **兼容已有数据**：应用名称和云端文件可以升级为 XYBox，但旧 XMBOX 本地数据及 WebDAV 文件必须有一次性迁移路径。

## 3. 当前代码核对结论

### 3.1 手机和平板实际上是两套构建版本

当前 Gradle 同时定义了 `mobile`、`tablet`、`leanback` 三个模式，并另外区分 `arm64_v8a` 与 `armeabi_v7a`，因此会产生很多构建组合。

手机和平板源集的实际情况：

- Mobile：100 个 Java 文件、247 个资源文件。
- Tablet：96 个 Java 文件、246 个资源文件。
- 两边有 338 个相同相对路径，其中 317 个文件内容完全一致，只有 21 个同路径文件存在差异。

结论：两套触屏版本没有继续分开维护的必要，适合合并成一个触屏端。

### 3.2 主题被代码强制锁定为深色

- `BaseActivity` 每次创建页面都会强制设置 `MODE_NIGHT_YES`。
- 主主题直接继承 `Theme.Material3.Dark.NoActionBar`。
- 很多布局仍直接使用白色、黑色等固定颜色，而不是语义化主题颜色。

因此仅增加“浅色模式”开关并不能完整解决问题，需要先建立统一的颜色语义层。

### 3.3 播放器手势与遮罩耦合

- 双击区域被硬编码为：左侧 20% 快退、中间 60% 播放/暂停、右侧 20% 快进。
- 快退和快进固定为 10 秒，当前没有用户开关。
- 中间双击暂停后会调用 `showControl()`。
- 完整控制层根布局带有 `black_20` 黑色遮罩，所以双击暂停会让视频画面变暗。
- 全屏返回、锁定、旋转目前共同放在屏幕右侧中间的竖向按钮组中。

### 3.4 WebDAV 自动同步没有真正接入执行路径

- WebDAV 设置界面可以保存“自动同步”和“同步间隔”，但这些值没有被应用启动或后台任务真正消费。
- App 当前启动和定时执行的是 `AutoSyncManager`，它同步的是局域网内另一台正在运行的设备，并不是 WebDAV。
- WebDAV 只在设置对话框中保存配置或手动点击时执行，因此会给用户造成“已经开启云同步”的错误预期。
- 当前流程是“先上传本地文件，再下载远程文件”，可能先覆盖掉另一台设备更新后的远程数据。
- 当前 WebDAV 文件仍以 `xmbox_*.json` 命名；部分下载代码还使用 `InputStream.available()` 判断长度，不适合网络流。
- “同步码 + Gist raw URL”不是可靠的可写 WebDAV 后端，不应继续作为看似可用的同步方式展示。

### 3.5 底部导航已经存在，但不是悬浮设计

手机和平板首页都有同样的 `BottomNavigationView`，目前是贴底、全宽、透明背景、高度 70dp。悬浮底栏不需要重新发明导航结构，重点是统一容器、圆角、边距、阴影、系统手势区域和内容避让。

### 3.6 播放通知使用了旧图标

- App 启动图标已经替换，但 `PlaybackService` 的小图标仍固定使用 `R.drawable.ic_logo`。
- 该文件仍是旧图形，所以状态栏和播放通知继续显示旧版标识。
- 通知渠道名称也仍固定写为 `XMBOX`。

Android 通知小图标需要单色透明图形，不能直接把彩色启动图标原样塞进状态栏。

## 4. 目标产品形态

### 4.1 当前发布物

本轮完成后，默认只产生：

```text
XYBox-debug.apk
XYBox-release.apk
```

- 不再区分 Mobile APK 和 Tablet APK。
- 当前自用发布默认只保留 `arm64-v8a`，使用普通的 `assembleDebug` / `assembleRelease` 构建。
- 如果以后确认存在必须支持的 32 位设备，可以把两个 ABI 放进同一个通用 APK，仍然不恢复多版本发布矩阵。
- Leanback/电视端不进入默认构建；未来如要继续，优先作为独立 TV 模块或独立发布目标，而不是重新复制整套触屏代码。

### 4.2 自适应规则

触屏端使用当前窗口宽度分级，而不是判断设备型号：

- Compact：小于 600dp，适合普通手机和窄分屏。
- Expanded：大于等于 600dp，适合平板、横屏手机和宽分屏。
- 横竖屏变化时重新计算卡片列数、间距和内容宽度。
- 首页导航保持统一的底部栏；Expanded 模式增加内容列数和留白，不另起一套平板页面。
- 播放页、设置页、弹窗等使用资源限定符或运行时尺寸参数处理必要差异，不复制业务类。

## 5. 实施阶段

### 阶段 0：保护基线与验证规则

状态：初始可编译基线已经完成。

后续要求：

- 以提交 `6244238` 作为永久回退点。
- 每个功能单独提交，不改动无关模块。
- 每阶段至少执行一次 Debug 编译。
- 涉及数据结构或同步算法时，先写合并逻辑测试，再连接真实 WebDAV。
- 不把 `local.properties`、签名文件、WebDAV 凭据和 APK 构建产物提交进 Git。

### 阶段 1：合并手机和平板，形成单一 APK

状态：已完成，提交 `d01e445`。

目标：先解决后续所有 UI 和交互修改需要重复开发的问题。

实施内容：

1. 以当前 Mobile 触屏端作为合并起点，对 21 个存在差异的同路径文件逐一选择或合并行为。
2. 把 Mobile/Tablet 中完全相同的 Java 与资源移动到共同触屏源集。
3. 把 `Product` 中卡片列数和尺寸逻辑改为读取当前窗口宽度。
4. 仅保留必要的 `layout-sw600dp`、`values-sw600dp` 等资源差异，不再复制 Activity、Fragment、Adapter 和 Dialog。
5. 合并两个 Manifest，核对横竖屏、画中画、分享、文件打开和通知权限声明。
6. 移除触屏端的 `mobile/tablet` 模式 flavor；当前默认 ABI 固定为 `arm64-v8a`。
7. 构建任务收敛为：

   ```powershell
   .\gradlew.bat assembleDebug
   ```

8. 电视端代码暂时不删除，先从默认构建中隔离，避免本轮误伤。

验收标准：

- 同一个 APK 可安装在手机和平板。
- 手机和平板都能完成首页、点播、直播、搜索、设置和播放基本流程。
- 旋转屏幕或进入分屏后不崩溃，内容列数和间距能重新适配。
- Git 中不再存在两份需要同步修改的触屏业务类。

### 阶段 2：品牌一致性与通知图标修复

状态：已完成，提交 `7e8f25d`、`d60d047`、`277eea0`。

目标：先完成低风险但用户可见的问题，同时建立统一品牌资源入口。

实施内容：

1. 将所有用户可见的应用名称统一为 `XYBox`，包括 App 名称、设置页标题、隐私协议标题和通知渠道名称。
2. 保留当前用户认可的启动图标，不重新设计图标。
3. 从当前 XYBox 图标提取适合 Android 状态栏规范的单色透明通知图标，例如 `ic_stat_xybox`。
4. `PlaybackService` 使用新的通知小图标；没有视频封面时使用 XYBox 品牌图作为大图标回退。
5. 同时检查下载通知、前台服务通知、锁屏媒体控件和通知操作按钮。
6. 本轮先保留内部 Java 包名和 `applicationId`，不为品牌改名进行全项目包迁移，避免影响已有数据、安装覆盖和第三方调用。

验收标准：

- 桌面、最近任务、状态栏、通知栏和锁屏媒体控件不再出现旧版 FongMi/XMBOX 图标或名称。
- Android 浅色与深色状态栏下，小图标都清晰可辨。
- 覆盖安装后原有本地数据仍然存在。

### 阶段 3：播放器操作与可配置手势

状态：已完成，提交 `859edca`、`ed40c22`。

目标：把播放操作从硬编码改为用户可以控制的策略。

新增“播放器手势”设置组：

- 双击中间播放/暂停：默认开启。
- 双击左右快退/快进：默认关闭，可开启。
- 快退/快进时长：5、10、15、30 秒，默认 10 秒。
- 左侧上下滑动亮度：保留开关。
- 右侧上下滑动音量：保留开关。
- 水平滑动进度：保留开关。

交互调整：

1. 双击中间只切换播放状态，并显示短暂的居中播放/暂停反馈图标。
2. 双击中间不再调用完整的 `showControl()`，因此不会触发全屏黑色遮罩。
3. 左右双击关闭时，该区域不执行快退或快进，也不能误触发其他动作。
4. 左右双击开启时，只显示局部的时间变化反馈，不强制显示整套控制栏。
5. 单击屏幕仍用于显示或隐藏完整控制栏；只有用户主动打开完整控制栏时才允许显示轻量遮罩。
6. 全屏返回按钮移动到左上角，并正确避开刘海、挖孔和系统状态栏。
7. 右侧中间只保留锁定与旋转等适合单手操作的按钮。
8. 手机、平板宽度和横竖屏使用同一套手势策略。

验收标准：

- 双击中间暂停后，视频亮度不发生变化，完整控制层不自动出现。
- 设置中关闭左右双击后，连续多次双击左右区域都不会改变进度。
- 5/10/15/30 秒步长与界面提示一致。
- 全屏左上角返回按钮可点击区域不小于 48dp，横竖屏和带刘海设备均可用。
- 锁屏模式下不会因手势误操作退出、旋转或改变进度。

### 阶段 4：重做 WebDAV 为真正的异步云同步

状态：同步 v2 核心实现和坚果云真机联调已完成；冷启动自动同步、连续双向同步均已通过，双设备冲突场景仍需用户在第二台设备上最终验收。

目标：设备 A 关闭后，设备 B 仍能从 WebDAV 获得 A 的更新；两台设备不需要同时打开 App。

#### 4.1 先纠正功能边界

- 将现有局域网设备同步明确命名为“局域网同步”，不再与 WebDAV 自动同步混用。
- WebDAV 页面只负责真正的云端持久化同步。
- 暂时隐藏或移除不可写的 Gist raw“同步码”模式，只保留真实 WebDAV 账号模式。
- 第一优先支持坚果云，同时保持标准 WebDAV 地址可配置。

#### 4.2 新的同步流程

禁止继续使用“先覆盖上传，再下载”的顺序。新的单次同步流程为：

```text
获取远程元数据和文件
        ↓
读取本地快照
        ↓
按记录 ID 和更新时间进行双向合并
        ↓
把合并结果写回本地
        ↓
带版本条件地上传合并结果
        ↓
保存本次远程版本、时间和结果
```

具体要求：

1. 新增带 `schemaVersion`、`revision`、`deviceId` 和 `updatedAt` 的同步格式。
2. 观看记录、收藏和允许同步的设置按条目合并，不再整文件盲目覆盖。
3. 删除操作使用墓碑记录（`deletedAt`），避免另一台设备把已删除内容重新同步回来。
4. 使用 ETag 或 Last-Modified 做条件更新；远程版本发生变化时重新拉取并合并。
5. 上传时优先使用临时文件加 WebDAV MOVE，或至少保留上一版备份，防止半个 JSON 文件损坏所有设备。
6. 下载网络流必须循环读取，不能依赖 `available()`。
7. 新格式使用 `xybox_sync_v2.json`；首次运行检测并导入旧的 `xmbox_history.json`、`xmbox_settings.json` 和 `xmbox_backup.json`，导入成功后不立即删除旧文件。
8. 设置采用白名单同步：主题、播放器手势、收藏和播放偏好可以同步；WebDAV 密码、设备路径、缓存目录和仅本机有效的设置不能上传。

#### 4.3 自动触发机制

- App 进程冷启动时：强制执行一次拉取、合并和必要上传；普通页面切换只按设定间隔检查。
- 观看进度或收藏发生变化时：延迟合并多次修改，再安排上传，避免每秒写云端。
- App 运行期间按用户设定的间隔同步；进程结束后不伪装常驻后台，下一次启动会立即补同步。后续确有长期后台需求时再引入 WorkManager。
- 网络恢复时：提交一次待处理同步。
- App 退出时：可以尝试快速提交，但不能把“退出回调”当作唯一同步机会。
- 设置页保留“立即同步”和“查看上次结果”。

#### 4.4 安全与可诊断性

- WebDAV 密码使用 Android Keystore 保护，不再以普通偏好明文保存。
- 日志不得输出密码、完整 URL 查询参数、完整观看记录 JSON 或用户片名列表。
- 设置页显示：上次成功时间、同步状态、本地/远程版本、最后错误原因。
- 失败采用有限指数退避，不无限快速重试。

验收场景：

1. 设备 A 播放到某进度并完成同步，然后彻底关闭 A；设备 B 稍后单独启动，能获取该进度。
2. A、B 离线时分别修改不同影片，恢复网络后两条修改都保留。
3. A、B 修改同一条记录时，按 `updatedAt` 选择较新记录，并能在日志中说明合并结果。
4. 一台设备删除收藏后，另一台设备不会把它重新上传回来。
5. 上传中断不会破坏上一份可读取的云端数据。
6. 坚果云使用应用密码时，连接测试、首次上传、再次下载和周期同步均成功。
7. 不开启两台 App 同时在线也能完成以上流程。

### 阶段 5：主题系统与悬浮底栏

目标：在单一触屏架构上完成统一视觉，避免分别修改手机和平板 UI。

#### 5.1 主题设置

第一版提供：

- 跟随系统。
- 浅色。
- 深色。
- 强调色预设：黄色、蓝色、绿色、紫色等经过对比度检查的有限色板。

实施内容：

1. 移除 `BaseActivity` 中强制深色的代码。
2. 使用 `AppCompatDelegate` 根据用户设置应用主题。
3. 将背景、卡片、正文、次要文字、分隔线、遮罩、选中态、危险操作等颜色改为语义化资源。
4. 清理布局中直接写死的白色、黑色和黄色，确保浅色主题不是简单反色。
5. 主题切换后当前页面安全重建，并保留播放进度和导航位置。
6. 播放器视频区域保持黑色背景，但控制层、弹窗和设置页遵循主题。

#### 5.2 悬浮底栏

实施内容：

1. 复用现有 BottomNavigationView，外层改为悬浮圆角容器。
2. 手机左右边距建议 16dp，Expanded 宽度下限制最大宽度并居中，避免平板上横跨整屏。
3. 底部位置基于系统手势/导航栏 Insets 计算，不写死贴底距离。
4. 增加适度圆角、表面色、描边或阴影；颜色从主题读取。
5. 内容容器增加底部安全间距，列表最后一项不能被悬浮栏遮住。
6. 保持点播、直播、设置三个入口；隐藏直播入口时，其余项目自动重新均分。
7. 每个导航项保留至少 48dp 触控区域、内容描述和清晰选中态。

验收标准：

- 手机和平板使用同一套悬浮底栏，Expanded 模式下不会显得过宽。
- 手势导航、三键导航、横屏和分屏时都不与系统区域重叠。
- 浅色、深色和跟随系统均无看不清的文字或图标。
- 切换主题不会丢失播放进度、当前 Tab 或用户设置。

### 阶段 6：稳定性、兼容性与发布收尾

实施内容：

1. 完整执行一次品牌文本检查，区分必须保留的上游署名与不应继续显示的旧产品名称。
2. 为自适应计算、手势策略和 WebDAV 合并算法补充单元测试。
3. 清理仅用于调试的高频日志，尤其是平板卡片尺寸和 WebDAV 数据日志。
4. 检查前台播放服务、通知权限、画中画、文件访问和下载在较新 Android 上的行为。
5. 当前 `targetSdk` 仍为 28，而 `compileSdk` 已是 36。目标 SDK 升级单独立项、逐级验证，不与本轮 UI 和同步改动混在同一次提交中。
6. 处理 Gradle 9 不兼容警告、旧 API 警告和 Room 构造函数警告，但不为消除警告改坏已工作的播放器。
7. 输出唯一的自用 Release APK，并记录版本号、提交号、SHA-256 和安装验证结果。

## 6. 测试矩阵

### 布局设备

- 普通手机：竖屏、横屏。
- 大屏手机或小平板：竖屏、横屏。
- 标准平板：竖屏、横屏。
- 分屏窗口：窄窗口、宽窗口。
- 带刘海/挖孔设备：播放器全屏控制。

### Android 版本

- 最低版本附近设备或模拟器。
- Android 10/11。
- Android 13：通知权限重点验证。
- Android 14/15：前台媒体服务、系统手势和文件访问重点验证。

### 核心回归

- 点播搜索、详情、换集、历史、收藏。
- 直播切台、回看和全屏控制。
- ExoPlayer 播放、投屏、画中画、字幕、弹幕。
- 下载、文件播放、WebDAV、配置导入导出。
- 覆盖安装与本地数据库保留。

## 7. 建议提交顺序

每一项完成并验证后再进入下一项：

1. `docs: add XYBox modification plan`
2. `refactor: unify mobile and tablet into adaptive handheld app`
3. `fix: align XYBox branding and playback notification icon`
4. `feat: add configurable player gestures and controls`
5. `feat: implement WebDAV sync v2`
6. `feat: add theme modes and floating bottom navigation`
7. `test: cover adaptive layout and sync conflict rules`
8. `chore: prepare single APK release`

## 8. 本轮明确不做

- 不开发电视版新 UI。
- 不增加 MPV 播放器。
- 不整体迁移 FongMi 新架构。
- 不重写 CatVod、QuickJS 或现有解码内核。
- 不在没有迁移方案的情况下修改 `applicationId`。
- 不把大量顺手发现的问题混入当前阶段；新问题进入缺陷清单，按风险排序处理。

## 9. 最终完成定义

以下条件全部满足，才视为本轮主要目标完成：

- 只有一个触屏 Android APK，手机和平板无需选择版本。
- 主题支持跟随系统、浅色和深色，并有可用的强调色设置。
- 左右双击快退/快进可关闭，步长可调。
- 双击中间暂停时画面不变暗。
- 全屏返回按钮位于左上角且不被系统区域遮挡。
- WebDAV 不依赖两台设备同时在线，离线修改和冲突不会简单互相覆盖。
- 手机和平板首页都使用统一的悬浮底栏。
- 播放通知和通知渠道完整显示 XYBox 品牌。
- 覆盖安装不丢失本地数据，旧 XMBOX WebDAV 数据可以迁移。
- Debug 和 Release 均能成功构建，核心回归流程通过。
